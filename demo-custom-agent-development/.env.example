# Dev Workflow Automation Hub — Environment Variables
# Copy this file to .env and fill in your actual values.
# Never commit .env to version control.

# ---------------------------------------------------------------------------
# GitHub
# ---------------------------------------------------------------------------

# Personal Access Token (fine-grained recommended)
# Create one at: https://github.com/settings/tokens?type=beta
# Required scopes: repo (read), issues (read), pull_requests (read)
GITHUB_PERSONAL_ACCESS_TOKEN=
